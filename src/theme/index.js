import { extendTheme } from "native-base";

export const theme = extendTheme({
 
    colors: {
      blue: {
        10: '#0D253C',
        20: '#2D4379',
        30: '#7B8BB2',
        40: '#376AED'
      },
    },

    
});